# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Index.main()

# API by simple name
GET     /api/health                 controllers.Api.named(name="HealthChecks")
GET     /api/metrics/all            controllers.Api.named(name="FetchMetrics")
POST    /api/metrics/clear          controllers.Api.named(name="ClearMachineMetrics")

GET     /admin                      controllers.Api.named(name="AdminPanel")

# Full path API calls
GET     /api/*path                  controllers.Api.api(path)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
